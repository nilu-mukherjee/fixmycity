import { addTodo, listTodos } from './todos'
import {
  analyzeReport,
  createTicket,
  getTicket,
  getUploadUrl,
  listTickets,
} from './reports'

export default {
  listTodos,
  addTodo,
  getUploadUrl,
  analyzeReport,
  createTicket,
  listTickets,
  getTicket,
}
